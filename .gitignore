node_modules
.next
next-env.d.ts
*.tsbuildinfo
.DS_Store
.vercel
.pnpm-store

# secrets — Payload and the database land here in phase 1
.env
.env*.local
!.env.example

# local upload target for the media collection; real uploads move to Blob in phase 2
# anchored so it cannot swallow a future src/**/media directory
/media/

# local tooling output
.playwright-mcp
.screens
*.log
.env*
