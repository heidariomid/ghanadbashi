# Archive

Superseded documents, kept for history. **Do not build from these** — they
describe directions the project no longer takes.

| File | What it was | Why it's here |
| --- | --- | --- |
| `prompt` | The original build brief | Specifies an illustration-led design with hand-drawn SVGs and explicitly *no photography*. The client approved the opposite: a photography-led design (`RTL Bakery Homepage Demo/`). Following this file would undo the approved look. |
| `chat` | Instructions accompanying that brief | Same reason. |
| `IMPLEMENTATION-PLAN.md` | The meta-plan for generating this template | Already executed — `context/` and the phase specs are its output. |

Current sources of truth: @context/project-overview.md for the plan,
`RTL Bakery Homepage Demo/` for the design.
